services:
  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: api_db
    ports:
      - "3306:3306"

  api:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - db